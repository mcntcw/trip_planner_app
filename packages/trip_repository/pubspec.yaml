name: trip_repository
description: Repository of trip
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  uuid: ^4.4.0
  google_places_flutter: ^2.0.8
  intl: ^0.19.0

  place_repository: 
    path: ../place_repository
  # group_repository: 
  #   path: ../group_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true